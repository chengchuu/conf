# blog reverse proxy
upstream weblist {
    server winlist:5244;
}
server {
    listen 80;
    server_name list.home list.local alist.local;

    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log warn;

    location / {
        proxy_set_header X-Real-IP        $remote_addr;
        proxy_set_header X-Forwarded-For  $proxy_add_x_forwarded_for;
        proxy_set_header Host             $host;
        proxy_set_header X-Nginx-Proxy    true;
        proxy_set_header Connection       "";
        proxy_pass http://weblist;
    }
}

# blog reverse proxy
upstream webblog {
    server host.docker.internal:82;
}
server {
    listen 80;
    server_name blog.mazey.net;

    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log warn;

    location / {
        proxy_set_header X-Real-IP        $remote_addr;
        proxy_set_header X-Forwarded-For  $proxy_add_x_forwarded_for;
        proxy_set_header Host             $host;
        proxy_set_header X-Nginx-Proxy    true;
        proxy_set_header Connection       "";
        proxy_pass http://webblog;
    }
}

# mazey reverse proxy
upstream webmazey {
    server host.docker.internal:81;
}
server {
    listen 80;
    server_name mazey.cn;

    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log warn;

    location / {
        proxy_set_header X-Real-IP        $remote_addr;
        proxy_set_header X-Forwarded-For  $proxy_add_x_forwarded_for;
        proxy_set_header Host             $host;
        proxy_set_header X-Nginx-Proxy    true;
        proxy_set_header Connection       "";
        proxy_pass http://webmazey;
    }
}

# local video server
server {
    listen 80 default_server;
    server_name win.home win.local;
    root /web/x/base;
    index index.html;

    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log warn;

    location /vid/ {
        alias /web/vid/;
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;

        sendfile on;
        tcp_nopush on;
        aio on;
        directio 8m;
        output_buffers 1 512k;

        mp4;
        mp4_buffer_size 512k;
        mp4_max_buffer_size 10m;

        types {
            video/mp4  mp4;
            video/webm webm;
            video/ogg  ogv;
            application/vnd.apple.mpegurl m3u8;
            video/mp2t ts;
        }

        gzip off;
        expires 7d;
        add_header Cache-Control "public, max-age=604800, immutable" always;
        add_header Accept-Ranges "bytes" always;

        add_header Access-Control-Allow-Origin "*" always;
        add_header Access-Control-Allow-Methods "GET, HEAD, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Range" always;
        add_header Access-Control-Expose-Headers "Content-Length,Content-Range" always;

        if ($request_method = OPTIONS) { return 204; }

        try_files $uri $uri/ =404;
    }

    location /list/ {
        proxy_http_version 1.1;
        proxy_set_header X-Real-IP        $remote_addr;
        proxy_set_header X-Forwarded-For  $proxy_add_x_forwarded_for;
        proxy_set_header Host             $host;
        proxy_set_header X-Nginx-Proxy    true;
        proxy_set_header Connection       "";
        proxy_pass http://weblist/;
    }
    location ~ ^/(assets|static|api|images)/ {
        proxy_pass http://weblist;
    }

    # /host/local.hosts
    location /host/ {
        alias /web/conf/config/hosthome/;
    }
}
